export type FeatureType = {
    feature_id: string,
    name: string,
    description: string,
    // add permission later
    fee_type: string,
    is_valid: string,
    created_at: string,
    updated_at: string
}